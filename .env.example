# Runtime GraphQL HTTP endpoint (used by RTK Query + graphql-request)
VITE_GRAPHQL_URL=http://localhost:3001/api/graphql

# Optional: introspection URL for `npm run codegen` (overrides local schema.graphql)
# CODEGEN_SCHEMA_URL=http://localhost:3001/api/graphql
