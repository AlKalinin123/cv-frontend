/**
 * Shared API-related types from the published schema package (inputs, enums).
 * Operation result types should come from GraphQL Codegen (`generated.ts`).
 */
export { Mastery, type AuthInput } from 'cv-graphql'
